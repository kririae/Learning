#include <bits/stdc++.h>
using namespace std;
const int maxn = 1e3 + 5;
int n, k;
int res = 0;
char s[maxn], t[maxn];
int main() {
	cin.tie(0);
	ios::sync_with_stdio(false);
	cin >> n >> k;
	cin >> s >> t;
	for () 
	cout << res - 1 << endl;
}

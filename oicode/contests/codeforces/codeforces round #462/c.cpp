#include <bits/stdc++.h>
using namespace std;
const int maxn = 2005;
int n;
int a[maxn];
int main() {
  cin.tie(0);
  ios::sync_with_stdio(false);
  cin >> n;
  for (int i = 1; i <= n; ++i) {
    cin >> a[i];
  }
}
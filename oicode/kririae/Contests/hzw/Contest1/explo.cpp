#include <bits/stdc++.h>

using namespace std;

const int maxn = 100005;
int n, k, c, w, a[maxn], t[maxn];

int main()
{
	cin.tie(0);
	ios::sync_with_stdio(false);
	
	cin >> n >> k >> c >> w;
	for (int i = 1; i <= n; ++i)
		cin >> t[i] >> a[i];

	
}
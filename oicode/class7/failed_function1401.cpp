#include<iostream>
#include<cstdio>
#include<cstring>
using namespace std;
int s[10000],m[10000];
int a,n,count=0,mana=0;
bool in(int num){
	//int length=sizeof(s)/sizeof(s[0]);
	bool if_in = false;
	for (int i = 0; i < 10000; ++i)
		if(num==s[i])if_in=true;
	return if_in;
}
void add(int num){
	int used=0;
	continue;
}
int main(){
	cin>>a>>n;
	memset(s,-1,sizeof(s));

	cout<<count;
	return 0;
}
/*
 * by kriaeth
 * time:
 * using template v0.0
 * read function by xehoth
 */

#include <bitset>
#include <cstdio>
#include <deque>
#include <iostream>
#include <queue>
#include <vector>


namespace solve {

template <typename T> auto min = [](T a, T b) { return a < b ? a : b; };

template <typename T> auto max = [](T a, T b) { return a < b ? b : a; };

inline void init() {}

inline void solve() {}
} // namespace solve

int main() {
  solve::solve();
  return 0;
}

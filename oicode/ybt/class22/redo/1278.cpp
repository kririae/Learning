#include <bits/stdc++.h>
using namespace std;
struct task {
	int x;
	int y;
	bool operator < (const task& a) const {
		return this -> x < a.x
	}
} t[10005];
int value[10005];
int main() {
	int n, k;
	cin >> n >> k;
	for (int i = 0; i < k; ++i) {
		cin >> p[i].x >> p[i].y;
	}
	sort(p, p + k);
	for(int i = 0; i <= n; ++i) {
		
	}
	return 0;
}

#include <bits/stdc++.h>
using namespace std;

int main()
{
	// freopen("tromino.in", "r", stdin);
	// freopen("tromino.out", "w", stdout);
	int n;
	scanf("%d", &n);
	if (n == 1)
		cout << 1;
	if (n == 2)
		cout << 3;
	if (n == 3)
		cout << 10;
}
